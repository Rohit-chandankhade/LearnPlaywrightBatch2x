let a = 10;
let b = 20;

console.log(a > b
    ? "maximum number is A"
    : "maximun numbers is B");


// using 3 variables 

let c = 30;
let d = 40;
let e = 50;

console.log(c > d
    ? "maxminum nmber is c" : (c > e ? "maximum number is c" : "maximum number is e"));


console.log(d > c
    ? (d > e ? "maximum number is d" : "max num is e") : "maximun nmber is d")