let name = "Rohit";

let printRohit = name === "Rohit"
    ? "Rohit is  printed"
    : "ROhit is not printed";

console.log(`"output", ${printRohit}, "correctly" `);


//**************************************** */ another example ****************************************8

let empNum = 109896;
let num = 200000;

let checkEmp = num <= empNum
    ? "old emp"
    : (num > 100000 ? "new emp exist" : "new emmp does not exist");

console.log(checkEmp);



console.log(typeof NaN);